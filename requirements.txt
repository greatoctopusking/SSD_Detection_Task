# SSD 项目依赖清单（Python 3.12）
# ------------------------------------------------------------------
# 1) 创建环境
#      conda create -n ssd python=3.12 -y && conda activate ssd
#
# 2) 安装 PyTorch —— 按机器选一种（先看 nvidia-smi 的 CUDA Version）
#    GPU（CUDA 12.x，按驱动选 cu121 / cu124 / cu126）：
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#    CPU：
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# 3) 安装其余依赖
#      pip install -r requirements.txt
#
# 说明：torch/torchvision 不写进本文件，避免在 GPU 机器上被 +cpu 轮子覆盖。
# ------------------------------------------------------------------

opencv-python>=4.10
numpy>=1.26
pycocotools>=2.0.7
PyYAML>=6.0
matplotlib>=3.8
tqdm>=4.66
pillow>=10.0

# 本机（CPU）已安装的版本，供参考：
#   torch 2.14.0+cpu / torchvision 0.29.0+cpu / numpy 2.5.2 / opencv-python 5.0.0.93
#   pycocotools 2.0.11 / PyYAML 6.0.3 / matplotlib 3.11.1 / tqdm 4.70.0
